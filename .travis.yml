language: r
warnings_are_errors: true
sudo: false

addons:
    apt:
        packages:
            - libgeos-dev
# NOTE: libgeo++-dev not on current travis-ci white list for container-based

matrix:
    include:
        - r: release
          env: DEPS=cran
          os: linux
          dist: trusty
        - r: release
          env: DEPS=cran
          os: osx
        - r: oldrel
          env: DEPS=cran
          os: osx

notifications:
  email:
    on_success: change
    on_failure: change
